<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Membership Form</title>
    <style>
        body {
            background: radial-gradient(circle, #1a1a1a, #000000);
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            color: #fff;
        }
        .navbar {
            background-color: #1a1a1a;
            padding: 15px;
            text-align: center;
            border-bottom: 2px solid #8B0000;
        }
        .navbar a {
            color: white;
            text-decoration: none;
            font-size: 18px;
            margin: 0 15px;
            transition: 0.3s;
        }
        .navbar a:hover {
            color: #8B0000;
        }
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 90vh;
            flex-direction: column;
        }
        .form-container {
            background: rgba(255, 255, 255, 0.1);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0px 0px 10px rgba(255, 255, 255, 0.2);
            width: 300px;
            text-align: center;
            backdrop-filter: blur(10px);
        }
        input {
            display: block;
            width: 100%;
            padding: 12px;
            margin: 12px 0;
            border: 1px solid #8B0000;
            border-radius: 8px;
            background: rgba(255, 255, 255, 0.2);
            color: #fff;
        }
        input::placeholder {
            color: #ddd;
        }
        button {
            width: 100%;
            padding: 12px;
            background: #8B0000;
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 16px;
            transition: 0.3s;
        }
        button:hover {
            background: #FF0000;
        }
        h2 {
            margin-bottom: 15px;
        }
        footer {
            background-color: #1a1a1a;
            color: white;
            text-align: center;
            padding: 10px;
            margin-top: 20px;
        }
    </style>
</head>
<body>

    <div class="container">
        <div class="form-container">
            <h2>Become a Member</h2>
            <form action="/submit" method="POST">
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" placeholder="Enter your email" required>
                
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" placeholder="Enter your password" required>
                
                <label for="age">Age:</label>
                <input type="number" id="age" name="age" placeholder="Enter your age" required>
                
                <label for="phone">Phone Number:</label>
                <input type="tel" id="phone" name="phone" placeholder="Enter your phone number" pattern="[0-9]{10}" maxlength="10" required>            
                <button type="submit">Join Now</button>
            </form>
            
        </div>
    </div>
    <script>
        document.querySelector('form').addEventListener('submit', function(event) {
            let phoneInput = document.getElementById('phone').value;
            if (!/^[0-9]{10}$/.test(phoneInput)) {
                alert('Please enter a valid 10-digit phone number.');
                event.preventDefault();
            }
        });

        function showPopup(event) {
        event.preventDefault(); 
        alert("Form submitted successfully!"); 
        event.target.submit(); 

    document.addEventListener("DOMContentLoaded", function () {
        const form = document.getElementById("joinForm"); 
        if (form) {
            form.addEventListener("submit", showPopup);
        }
    });
    </script>
    <footer>
        &copy; MEMBER PAGE OF NDA AUTO EMPORIUM
    </footer>
</body>
<script>
    function showPopup(event) {
        event.preventDefault(); // Prevents the form from submitting immediately
        alert("Your form has been submitted successfully!");
        document.getElementById("memberForm").submit(); // Submit the form after showing the popup
    }
</script>
</html>
